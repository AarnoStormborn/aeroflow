# Minimal runtime requirements for the ingestion service on Raspberry Pi (DietPi, 1GB RAM)
# Generated from actual imports in ingestion/src — no dev tools, no pyarrow/jupyter/pyyaml
# Written for pip (NOT uv) to minimize footprint on the Pi.

httpx>=0.27.0
boto3>=1.34.0
polars>=1.0.0
loguru>=0.7.0
python-dotenv>=1.0.0
pydantic>=2.12.0
pydantic-settings>=2.12.0
apscheduler>=3.11.0
