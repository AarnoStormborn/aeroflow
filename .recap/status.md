# Aeroflow (Flights Forecasting Services) — Status Report

> Generated by project-recap on 2026-03-31 at 10:15.

## Project meta

- **Repo root**: `/Users/harshsingh/Documents/personal/aeroflow`
- **Language(s) / toolchain**: Python 3.10+, uv, Polars, PyArrow, scikit-learn, MLflow, Docker Compose
- **Key manifests**:
  - `ingestion/pyproject.toml`
  - `feature-engineering/pyproject.toml`
  - `model-training/pyproject.toml`
  - `model-training/docker-compose.yml`
- **Entrypoints**:
  - `ingestion/main.py` & `ingestion/ingestion_script.sh`
  - `feature-engineering/src/pipeline/run.py` & `feature-engineering/daily_report_script.sh`
  - `model-training/src/training/train.py` & `model-training/train_script.sh`
- **Structure**: Monorepo split into 3 microservices (`ingestion/`, `feature-engineering/`, `model-training/`) connected by S3 data partitions.
- **Docs**: Clean root README.md and service-level READMEs and markdown docs present.

## Git health

- **Branch**: `main` — up to date with `origin/main`
- **HEAD**: `d7a8023` `modify scripts` (AarnoStormborn, 2025-12-31)
- **Uncommitted**: 0 changed / 0 staged / 0 untracked (clean working tree before recap docs)
- **Remote**: `git@github.com:AarnoStormborn/aeroflow.git`
- **Recent commit activity**:
  - `d7a8023` modify scripts
  - `ae4530c` containers setup
  - `de101af` mlflow docker server
  - `23ada0c` model-training pipeline
  - `60aa125` feature-engineering; features produced

## Tests & build

- **Detected commands**: `uv run pytest` per microservice directory
- **Result**:
  - `ingestion`: **PASS** (6 passed in `tests/pipeline/test_failure_capture.py`)
  - `feature-engineering`: **NOT RUN / NO TESTS** (0 tests collected)
  - `model-training`: **NOT RUN / NO TESTS** (0 tests collected)

## Code scan

- **TODO/FIXME/HACK**: 1 total
  - `feature-engineering/main.py:10` — `print("TODO: Implement feature engineering pipeline")`
- **Dependencies**:
  - `ingestion`: polars, boto3, pyarrow, httpx, python-dotenv, loguru, pydantic, pydantic-settings, pyyaml, apscheduler
  - `feature-engineering`: polars, boto3, pydantic, pydantic-settings, python-dotenv, loguru, httpx, matplotlib, seaborn, jupyter, reportlab, scikit-learn, xgboost
  - `model-training`: polars, boto3, pydantic, pydantic-settings, python-dotenv, loguru, scikit-learn, mlflow, matplotlib, seaborn
- **Docs coverage**: Comprehensive READMEs in all 3 microservices plus architecture documentation in `ingestion/docs/`.
- **File types (top)**:
  - Python (`.py`): 51 files
  - Sample outputs (`.sample`, `.json`): 13 files
  - Markdown (`.md`): 6 files
  - Shell scripts (`.sh`): 4 files
  - Config (`.toml`, `.yml`, `.yaml`): 5 files

## Risks & attention points

- **Zero Test Coverage on Core Feature & Training Logic**: `feature-engineering` and `model-training` modules have test runners configured in `pyproject.toml` but currently have zero automated test cases.
- **Deprecated UV Config Syntax**: `pyproject.toml` files use deprecated `tool.uv.dev-dependencies` syntax, which uv warns will be removed in future releases.
- **Placeholder Entrypoint**: `feature-engineering/main.py` contains an incomplete placeholder script, while the active entrypoint lives in `src/pipeline/run.py`.

## Suggested next actions

1. Write unit tests for feature transformations in `feature-engineering/tests/` and model validation in `model-training/tests/`.
2. Clean up `feature-engineering/main.py` or redirect it to invoke `src.pipeline.run`.
3. Update `pyproject.toml` configurations across sub-projects to use modern `[dependency-groups] dev = [...]`.
4. Set up a GitHub Actions workflow to run linting and tests across all microservices on pull requests.
